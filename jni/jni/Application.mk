APP_ABI := armeabi-v7a
APP_PLATFORM := android-21
APP_STL := c++_static
APP_CPPFLAGS := -std=c++20 -fexceptions -frtti
APP_CFLAGS := -fexceptions -fvisibility=hidden -fpack-struct=1
APP_OPTIM := release
NDK_TOOLCHAIN_VERSION := clang
