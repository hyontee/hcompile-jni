ShadowHook integration for this project

Required files:
  include/shadowhook.h
  prebuilt/armeabi-v7a/libshadowhook.so

Android.mk already imports this library as module `shadowhook`.

Official repository:
https://github.com/bytedance/android-inline-hook

This project archive did not contain ShadowHook itself, so the .so/header files are intentionally not fabricated.
